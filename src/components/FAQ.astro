---
import { Sparkles, Info, ChevronDown, MessageCircle } from '@lucide/astro';
import { faqData } from '../data/faq';
import { siteConfig } from '../config/site.config';

const whatsappUrl = `https://wa.me/${siteConfig.contact.whatsappNumber}?text=${encodeURIComponent('Hola Origami, tengo una consulta sobre sus creaciones.')}`;
---

<section
  id="faq"
  aria-labelledby="faq-heading"
  class="scroll-mt-16 sm:scroll-mt-20 py-20 sm:py-28 bg-white border-b border-origami-cream-200/80 relative"
>
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Encabezado de Sección -->
    <div class="text-center max-w-3xl mx-auto mb-14">
      <div class="inline-flex items-center gap-2 px-3.5 py-1.5 rounded-full bg-origami-rose-100 text-origami-rose-800 text-xs font-semibold uppercase tracking-wider mb-4">
        <Sparkles class="w-3.5 h-3.5 text-origami-rose-600" />
        <span>Respuestas Claras</span>
      </div>
      <h2
        id="faq-heading"
        class="text-3xl sm:text-4xl lg:text-5xl font-extrabold tracking-tight text-origami-slate-900 font-serif"
      >
        Preguntas Frecuentes
      </h2>
      <p class="mt-4 text-base sm:text-lg text-origami-slate-600 leading-relaxed">
        Todo lo que necesitas saber sobre compras, personalización, tiempos y entregas antes de realizar tu pedido.
      </p>
    </div>

    <!-- Lista de Preguntas con Acordeón Exclusivo y Animación Fluida -->
    <div class="space-y-4" id="faq-accordion-container">
      {faqData.map((faq, index) => (
        <div
          class={`faq-item group bg-origami-cream-50 rounded-2xl border border-origami-cream-200 shadow-xs transition-all duration-300 overflow-hidden hover:border-origami-rose-300 ${index === 0 ? 'is-open' : ''}`}
        >
          <button
            type="button"
            id={`faq-btn-${index}`}
            aria-expanded={index === 0 ? 'true' : 'false'}
            aria-controls={`faq-panel-${index}`}
            class="faq-trigger w-full flex items-center justify-between p-6 cursor-pointer select-none text-left font-bold text-origami-slate-900 hover:text-origami-rose-700 transition-colors focus-visible:ring-2 focus-visible:ring-origami-lavender-700 rounded-2xl"
          >
            <span class="text-base sm:text-lg flex items-center gap-3 pr-4 font-serif">
              <Info class="w-5 h-5 text-origami-rose-600 shrink-0" />
              {faq.question}
            </span>
            <ChevronDown class="faq-chevron w-5 h-5 text-origami-slate-400 shrink-0" />
          </button>
          
          <div
            id={`faq-panel-${index}`}
            role="region"
            aria-labelledby={`faq-btn-${index}`}
            class="faq-content-wrapper"
          >
            <div class="overflow-hidden">
              <div class="px-6 pb-6 pt-2 text-sm sm:text-base text-origami-slate-600 leading-relaxed border-t border-origami-cream-200/80 bg-white/70">
                {faq.answer}
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Banner de Dudas Adicionales -->
    <div class="mt-12 p-6 sm:p-8 bg-origami-cream-100 rounded-3xl border border-origami-cream-200 flex flex-col sm:flex-row items-center justify-between gap-6 shadow-paper">
      <div class="text-center sm:text-left">
        <h3 class="text-base sm:text-lg font-bold text-origami-slate-900 font-serif">
          ¿Tienes una pregunta diferente o un diseño especial?
        </h3>
        <p class="text-xs sm:text-sm text-origami-slate-600 mt-1">
          Escríbenos directamente por WhatsApp y te orientamos al instante de forma personalizada.
        </p>
      </div>

      <a
        href={whatsappUrl}
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 px-5 py-3 rounded-2xl bg-emerald-700 hover:bg-emerald-600 text-white font-bold text-xs sm:text-sm transition-colors shadow-paper shrink-0"
      >
        <MessageCircle class="w-4 h-4" />
        Escribir a WhatsApp
      </a>
    </div>

  </div>
</section>

<script>
  // Control de acordeón exclusivo con animación CSS Grid continua y fluida
  const accordionContainer = document.getElementById('faq-accordion-container');
  if (accordionContainer) {
    const items = accordionContainer.querySelectorAll<HTMLElement>('.faq-item');

    items.forEach((item) => {
      const trigger = item.querySelector<HTMLButtonElement>('.faq-trigger');
      if (!trigger) return;

      trigger.addEventListener('click', () => {
        const isOpen = item.classList.contains('is-open');

        // Cerrar todos los items (garantizando exclusividad con animación suave simultánea)
        items.forEach((otherItem) => {
          const otherTrigger = otherItem.querySelector<HTMLButtonElement>('.faq-trigger');
          otherItem.classList.remove('is-open');
          if (otherTrigger) {
            otherTrigger.setAttribute('aria-expanded', 'false');
          }
        });

        // Si el elemento clickeado estaba cerrado, abrirlo con animación fluida
        if (!isOpen) {
          item.classList.add('is-open');
          trigger.setAttribute('aria-expanded', 'true');
        }
      });
    });
  }
</script>
